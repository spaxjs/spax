{
  "version": "independent",
  "packages": [
    "examples/*",
    "packages/*"
  ],
  "lerna": "3.15.0",
  "npmClient": "yarn",
  "ignoreChanges": [
    "examples/**",
    "**/test/**",
    "**/*.md"
  ],
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "chore(release): publish"
    },
    "bootstrap": {
      "ignore": "component-*",
      "npmClientArgs": ["--no-package-lock"]
    }
  }
}
