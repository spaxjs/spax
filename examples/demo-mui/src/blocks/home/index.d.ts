import UI from "./UI";
declare const _default: {
    layout: string;
    authority: string[];
    component: typeof UI;
    data: {
        title: string;
    };
};
/**
 * 首页，`path` 不填，默认为 `/`
 */
export default _default;
